# TODO

## Copilot/Agent

- Adapt mobile web browser.
  - `jobs.html`, combo box with right panel moved to below.
  - A separate mobile-friendly UI definition of quality.
- `report_intent` tool should apply `RESULT.intent` with the title (not just `Tool (INTENT)` but `INTENT` itself).
- Remove empty `Message` blocks after the block is closed.
- **TODO** in `API_Session.md`
- Readonly local git viewer, including diff rendering for untracked/staged/committed.
  - Code review UI for `Copilot_*.md` and run the dedicated follow up task.
