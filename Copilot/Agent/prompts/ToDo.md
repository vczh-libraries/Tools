# TODO

- Adapt mobile web browser.
  - `jobTracking.html`:
    - double touch point zooming in/out
    - hide session response panel when loading
    - clicking a task node shows the session response panel, occupying the whole page
    - a back button at the end of the tab header.
  - `jobs.html`
- `report_intent` tool should apply `RESULT.intent` with the title (not just `Tool (INTENT)` but `INTENT` itself).
- Replace gemini with sonnet in review prompt.
- More precise file names under `Reviewer Board Files` instead of just saying `*`.

- **TODO**L
  - `API_Session.md`
