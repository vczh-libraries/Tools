# TODO

- Adapt mobile web browser.
  - `jobs.html`
- `report_intent` tool should apply `RESULT.intent` with the title (not just `Tool (INTENT)` but `INTENT` itself).
- **TODO** in `API_Session.md`
- A plan mode to initiate `Copilot_Scrum.md` with iterations.
  - Could be a task running in session mode but ask agent to write everything to a temporary plan file.
  - Followed by another task to write `Copilot_Scrum.md` using the format.
  - Put these tasks before every other tasks, and make a dedicated UI after prompts are ready.
